import { createStore } from 'vuex'

export default createStore({
  state: {
    // logs: []
  },
  mutations: {
    // addLog(state, item) {
    //   state.logs.push(item)
    // },
  },
  actions: {
  },
  modules: {
  }
})
